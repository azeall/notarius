# Public demo is the default. Set live only after completing DEPLOYMENT.md.
NEXT_PUBLIC_BOOKING_MODE=demo
NEXT_PUBLIC_SITE_URL=
# template: demo uses https://app.guidecode.ru/widget/demo without env setup.
# These credentials are used only when NEXT_PUBLIC_BOOKING_MODE=live.
NEXT_PUBLIC_NOTARYBOT_URL=
NEXT_PUBLIC_NOTARYBOT_SLUG=
# Lavender / Warm / Modern only: configured isolated database and credentials
DATABASE_URL=
BOOKING_ENABLED=false
SESSION_SECRET=
ADMIN_PASSWORD=
STAFF_1_USER=helper1
STAFF_1_PASS=
TRUST_PROXY_IP=false
