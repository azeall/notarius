# Задачи: приёмка связки «сайт + виджет»

Отметка `[x]` ставится только после проверки, а не после правки.

## Сделано

- [x] **T001** Перенести инструментарий spec-kit на ветку (`.specify/` из `site-warm`).
- [x] **T002** Написать конституцию варианта: его роль — эталон интеграции виджета,
      а не пятый оттенок.
- [x] **T003** Написать спецификацию приёмки перед продажей.
- [x] **T004** Пометить отзывы как вымышленные. Раздел выводил карточки со звёздами
      и среднюю оценку без единого слова о том, что они сочинены.
- [x] **T005** Проверить открытием, что виджет работает: окно раскрылось, адрес кадра
      `https://app.guidecode.ru/widget/demo`.
- [x] **T006** Проверить, что адрес сервиса берётся из `lib/notarybot.ts`,
      а не зашит в компонент.
- [x] **T007** Проверить политику: паспортные данные названы, обработка по поручению
      описана, ложной фразы «не передаёт третьим лицам» нет, ссылка на согласие есть.

## Осталось

- [ ] **T008** Провести заявку до конца и убедиться, что она появилась у сотрудника.
      Проверка на демонстрационном нотариусе; тестовую запись после этого удалить.
- [ ] **T009** Проверить запасной путь: сделать сервис недоступным (например, подменив
      адрес на несуществующий) и убедиться, что окно закрывается само и появляется
      телефон конторы. Это единственный пункт, который нельзя проверить, пока
      всё работает.
- [ ] **T010** Проверить, что повторное нажатие после неудачи пробует загрузить виджет
      заново, а не показывает застрявшую ошибку.
- [ ] **T011** Проверить 390 px: нет горизонтального переполнения, виджет открывается
      и работает.
- [ ] **T012** Убедиться, что домен сайта внесён в `allowed_origins` нотариуса
      на стороне сервиса. Без этого `frame-ancestors` не пустит виджет в iframe,
      и сломается это молча — на чужом сайте, а не на нашем.

## Открытый вопрос

- [ ] **T013** Описать поведение, когда виджет открылся, но сервис вернул ошибку уже
      внутри окна. Событие `notarybot:unavailable` приходит только при отсутствии
      приветствия; поломка внутри кадра сейчас никак не обрабатывается.
